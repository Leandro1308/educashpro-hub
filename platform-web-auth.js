(function(){
  const DEFAULT_API_BASE="https://educashpro-all.onrender.com";
  const platform=window.EduCashProPlatform;
  function cleanBase(value){return String(value||DEFAULT_API_BASE).trim().replace(/\/+$/,"")}
  async function jsonFetch(url,options={}){const response=await fetch(url,{...options,headers:{"Content-Type":"application/json",...(options.headers||{})}});let data=null;try{data=await response.json()}catch{data={ok:false,reason:"invalid_json_response"}}if(!response.ok){const error=new Error(String(data?.message||data?.reason||`HTTP_${response.status}`));error.status=response.status;error.data=data;throw error}return data}
  function saveSession(result){if(!result?.token)return null;const session={token:result.token,profile:result.profile||null,storedAt:Date.now()};platform?.writeWebSession?.(session);if(result?.profile?.userId)platform?.clearPendingReferral?.();return session}
  async function requestChallenge({apiBase=DEFAULT_API_BASE,referralCode}={}){const ref=String(referralCode||platform?.pendingReferral?.()||"").trim();return jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/challenge`,{method:"POST",body:JSON.stringify({referralCode:ref})})}
  async function verifyWalletProof({apiBase=DEFAULT_API_BASE,challengeId,wallet,language}={}){const tonProof=wallet?.connectItems?.tonProof;if(!wallet?.account)throw new Error("wallet_account_missing");if(!tonProof||!("proof" in tonProof))throw new Error("ton_proof_not_supported");const result=await jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/verify`,{method:"POST",body:JSON.stringify({challengeId,account:wallet.account,proof:tonProof.proof,language:String(language||navigator.language||"pt").slice(0,8)})});saveSession(result);return result}
  async function prepareWalletAuthentication(tonConnectUI,{apiBase=DEFAULT_API_BASE}={}){if(!tonConnectUI?.setConnectRequestParameters)throw new Error("ton_connect_ui_required");tonConnectUI.setConnectRequestParameters({state:"loading"});try{const challenge=await requestChallenge({apiBase});if(!challenge?.challengeId||!challenge?.payload)throw new Error("invalid_auth_challenge");tonConnectUI.setConnectRequestParameters({state:"ready",value:{tonProof:challenge.payload}});return challenge}catch(error){tonConnectUI.setConnectRequestParameters(null);throw error}}
  function watchWalletAuthentication(tonConnectUI,{apiBase=DEFAULT_API_BASE,getChallenge}={}){if(!tonConnectUI?.onStatusChange)throw new Error("ton_connect_ui_required");return tonConnectUI.onStatusChange(async wallet=>{if(!wallet)return;const tonProof=wallet?.connectItems?.tonProof;if(!tonProof||!("proof" in tonProof))return;const challenge=typeof getChallenge==="function"?getChallenge():null;if(!challenge?.challengeId)return;try{const result=await verifyWalletProof({apiBase,challengeId:challenge.challengeId,wallet});window.dispatchEvent(new CustomEvent("educashpro:web-authenticated",{detail:result}))}catch(error){window.dispatchEvent(new CustomEvent("educashpro:web-auth-error",{detail:{message:error?.message||"web_auth_failed",status:error?.status||0,reason:error?.data?.reason||null}}))}})}
  async function authenticateFromTelegram({apiBase=DEFAULT_API_BASE}={}){const initData=platform?.telegramInitData?.()||"";if(!initData)throw new Error("telegram_initdata_missing");const result=await jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/from-telegram`,{method:"POST",body:JSON.stringify({initData})});saveSession(result);return result}
  async function linkTelegramToStoredSession({apiBase=DEFAULT_API_BASE}={}){const session=platform?.readWebSession?.();if(!session?.token)throw new Error("web_session_missing");const initData=platform?.telegramInitData?.()||"";if(!initData)throw new Error("telegram_initdata_missing");const result=await jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/link-telegram`,{method:"POST",headers:{Authorization:`Bearer ${session.token}`},body:JSON.stringify({initData})});const updated={...session,profile:result.profile||session.profile||null,storedAt:Date.now()};platform?.writeWebSession?.(updated);return result}
  async function validateStoredSession({apiBase=DEFAULT_API_BASE}={}){const session=platform?.readWebSession?.();if(!session?.token)return null;try{const result=await jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/session`,{method:"POST",headers:{Authorization:`Bearer ${session.token}`},body:"{}"});const updated={...session,profile:result.profile||session.profile||null,server:result.session||null};platform?.writeWebSession?.(updated);return updated}catch{platform?.writeWebSession?.(null);return null}}
  async function startDevicePairing({apiBase=DEFAULT_API_BASE}={}){return jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/pair/start`,{method:"POST",body:"{}"})}
  async function approveDevicePairing(code,{apiBase=DEFAULT_API_BASE}={}){const session=platform?.readWebSession?.();if(!session?.token)throw new Error("web_session_missing");return jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/pair/approve`,{method:"POST",headers:{Authorization:`Bearer ${session.token}`},body:JSON.stringify({code:String(code||"").replace(/\D/g,"").slice(0,6)})})}
  async function checkDevicePairing(pair,{apiBase=DEFAULT_API_BASE}={}){const result=await jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/pair/status`,{method:"POST",body:JSON.stringify({pairId:pair?.pairId,pairSecret:pair?.pairSecret})});if(result?.token)saveSession(result);return result}
  window.EduCashProWebAuth={requestChallenge,prepareWalletAuthentication,watchWalletAuthentication,verifyWalletProof,authenticateFromTelegram,linkTelegramToStoredSession,validateStoredSession,startDevicePairing,approveDevicePairing,checkDevicePairing,saveSession};
})();
