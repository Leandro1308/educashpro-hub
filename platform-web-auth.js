(function(){
  const DEFAULT_API_BASE="https://educashpro-all.onrender.com";
  const platform=window.EduCashProPlatform;
  function cleanBase(value){return String(value||DEFAULT_API_BASE).trim().replace(/\/+$/,"")}
  async function jsonFetch(url,options={}){const response=await fetch(url,{...options,headers:{"Content-Type":"application/json",...(options.headers||{})}});let data=null;try{data=await response.json()}catch{data={ok:false,reason:"invalid_json_response"}}if(!response.ok){const error=new Error(String(data?.message||data?.reason||`HTTP_${response.status}`));error.status=response.status;error.data=data;throw error}return data}
  function saveSession(result){if(!result?.token)return null;const session={token:result.token,profile:result.profile||null,storedAt:Date.now()};platform?.writeWebSession?.(session);if(result?.profile?.userId&&result?.pendingReferralCode===null)platform?.clearPendingReferral?.();return session}
  async function requestChallenge({apiBase=DEFAULT_API_BASE,referralCode}={}){const ref=String(referralCode||platform?.pendingReferral?.()||"").trim();return jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/challenge`,{method:"POST",body:JSON.stringify({referralCode:ref})})}
  async function verifyWalletProof({apiBase=DEFAULT_API_BASE,challengeId,wallet}={}){const tonProof=wallet?.connectItems?.tonProof;if(!wallet?.account)throw new Error("wallet_account_missing");if(!tonProof||!("proof" in tonProof))throw new Error("ton_proof_not_supported");const result=await jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/verify`,{method:"POST",body:JSON.stringify({challengeId,account:wallet.account,proof:tonProof.proof})});saveSession(result);return result}
  async function prepareWalletAuthentication(tonConnectUI,{apiBase=DEFAULT_API_BASE}={}){if(!tonConnectUI?.setConnectRequestParameters)throw new Error("ton_connect_ui_required");tonConnectUI.setConnectRequestParameters({state:"loading"});try{const challenge=await requestChallenge({apiBase});if(!challenge?.challengeId||!challenge?.payload)throw new Error("invalid_auth_challenge");tonConnectUI.setConnectRequestParameters({state:"ready",value:{tonProof:challenge.payload}});return challenge}catch(error){tonConnectUI.setConnectRequestParameters(null);throw error}}
  function watchWalletAuthentication(tonConnectUI,{apiBase=DEFAULT_API_BASE,getChallenge}={}){if(!tonConnectUI?.onStatusChange)throw new Error("ton_connect_ui_required");return tonConnectUI.onStatusChange(async wallet=>{if(!wallet)return;const challenge=typeof getChallenge==="function"?getChallenge():null;if(!challenge?.challengeId)return;try{const result=await verifyWalletProof({apiBase,challengeId:challenge.challengeId,wallet});window.dispatchEvent(new CustomEvent("educashpro:web-authenticated",{detail:result}))}catch(error){window.dispatchEvent(new CustomEvent("educashpro:web-auth-error",{detail:{message:error?.message||"web_auth_failed",status:error?.status||0,reason:error?.data?.reason||null}}))}})}
  async function authenticateFromTelegram({apiBase=DEFAULT_API_BASE}={}){const initData=platform?.telegramInitData?.()||"";if(!initData)throw new Error("telegram_initdata_missing");const result=await jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/from-telegram`,{method:"POST",body:JSON.stringify({initData})});saveSession(result);return result}
  async function validateStoredSession({apiBase=DEFAULT_API_BASE}={}){const session=platform?.readWebSession?.();if(!session?.token)return null;try{const result=await jsonFetch(`${cleanBase(apiBase)}/api/platform-auth/session`,{method:"POST",headers:{Authorization:`Bearer ${session.token}`},body:"{}"});return {...session,server:result.session||null}}catch{platform?.writeWebSession?.(null);return null}}
  window.EduCashProWebAuth={requestChallenge,prepareWalletAuthentication,watchWalletAuthentication,verifyWalletProof,authenticateFromTelegram,validateStoredSession,saveSession};
})();
